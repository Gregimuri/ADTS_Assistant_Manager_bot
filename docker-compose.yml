services:
  bot:
    build: .
    image: adts-assistant-manager:latest
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # Утренние планы выходов и снимки сборки переживают rebuild
      - bot-data:/app/data

volumes:
  bot-data:
