services:
  bot:
    build: .
    image: adts-assistant-manager:latest
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # Переживает rebuild/push: участники @all и утренние планы
      - bot-data:/app/data

volumes:
  bot-data:
